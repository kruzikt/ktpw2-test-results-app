<!DOCTYPE html>
<html lang="en">
  <%- include("../partials/head") %>

  <body class="d-flex flex-column vh-100">
    <%- include("../partials/navbar") %>
    <main class="container mt-5">
        <h1>Test title: <%= test.title%></h3>
        <h2>Author: Created by <%= test.author.username %></h2>
        <h2>Steps</h2> 
        <p><%- test.description%></p>

        <% if (currentUser && currentUser._id.equals(test.author._id)) { %>
          <a class="btn btn-info m-2" href="/tests/<%=test._id%>/edit">Edit test</a>
          <form action="/tests/<%= test._id%>?_method=DELETE" method="post">
            <button class="btn btn-danger m-2">Delete test</button>
          </form>
        <% } %> 

        <a class="btn btn-primary m-2" href="/tests">Back to index</a>
    </main>
    <%- include("../partials/footer") %>
  </body>
</html>
